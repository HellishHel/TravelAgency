var config = config || {};

config.methodName = 'types';

config.deleteErrorMessage = 'Невозможно удалить данный тариф, так как уже существуют заявки с ним';

config.searchDataPromise = [];
config.exceptFields = ['id'];